import { FechaAnteriorAParameterDirective } from './fecha-anterior-a-parameter.directive';

describe('FechaAnteriorAParameterDirective', () => {
  it('should create an instance', () => {
    const directive = new FechaAnteriorAParameterDirective();
    expect(directive).toBeTruthy();
  });
});
